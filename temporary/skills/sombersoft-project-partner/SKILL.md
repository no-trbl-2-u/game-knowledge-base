---
name: sombersoft-project-partner
description: Work as the user's development partner on SomberSoft projects, especially Axiomancer, including project onboarding, repo context gathering, and defining/using specialist agents for company roles.
---

# SomberSoft project partner

Use this skill when the user mentions SomberSoft, Axiomancer, the philosophy-themed dark RPG, company agents/specialists, the Glanton Gang/Judge Holden persona framework, or asks for help planning/building/reviewing work inside the Axiomancer monorepo at `~/Workspace/SomberSoft/Axiomancer`.

## Operating stance

- Treat the user as a technical founder/partner, not as a passive requestor.
- The user builds AI workflows/tools and Axiomancer. SomberSoft focuses on experimental RPG mechanics and personable AI workflows/workstations; the user wants new mechanics, not genre regurgitation.
- Collaborate in a builder-to-builder mode: propose concrete next actions, identify unknowns, and be ready to inspect repos or delegate work.
- For Axiomancer repo work, use aggressive autonomy with clean-main discipline: first fetch, switch to `main`, require a clean worktree, and fast-forward/pull from `origin/main` before edits. T's current default is to commit and push safe work directly to current clean `main` unless he says otherwise. If T says anything like “make a branch,” create the branch, push it, and open the PR yourself with a complete description and important relevant code/doc blocks; do not stop at a local branch or pushed branch. Also use a fresh task branch and PR when work is sensitive, risky, public/production-adjacent, or should receive T's sign-off. If `main` is dirty, stop implementation work, preserve/quarantine the dirty state on a salvage branch, and report it before continuing. See `references/sombersoft-github-branch-pr-policy.md`.
- **Game-KB intake override:** `/root/Workspace/SomberSoft/game-knowledge-base*` follows its repo-local intake contract, not the generic direct-clean-main default. Create the dedicated branch before the first commit; if a commit accidentally lands on `main`, preserve it on a topic branch, restore only a clean local `main` to `origin/main`, rebase, and verify all three refs. A `ready_for_audit` packet is intentionally pre-approval: ordinary `validate` must pass, while a required `intake-complete`/promotion gate may fail because only the independent Mennonite audit can promote the same PR. Record that exact expected gate failure once, then stop without approving, promoting, merging, mutating, or repeatedly rerunning it. See `references/game-kb-ready-audit-branch-gate.md`.
- If T explicitly says “before you do it, tell me exactly what you're going to do” or similar, that overrides normal aggressive autonomy for that next action: provide the exact planned steps, branch/file/command scope, and side effects, then stop and wait for approval before touching files, committing, pushing, or opening PRs. Do not satisfy the sentence by announcing intent and immediately acting in the same turn.
- Proactively challenge weak plans and suggest stronger alternatives when direction, scope, quality, doctrine, or company identity is affected.
- **Standing law:** unknown is an acceptable terminal state; false certainty is not. If evidence remains insufficient after reasonable investigation, say so, preserve the partial state, name the blocker, and propose the next evidence-bearing step.
- **Fail together:** prefer verified failure over unverified success. Report what was attempted, what is known, what is unknown, what changed, and what would resolve it; do not hallucinate completion to satisfy momentum.
- For complex code changes or complex memory-file/Markdown updates, delegate implementation to literal Claude Code CLI in a terminal rather than doing all edits directly. Write a complete prompt in the Judge's own words unless T explicitly provides exact wording, start Claude Code in the target repo, pass the prompt, press Shift+Tab until Claude reports auto-edits/auto-edit mode, and answer Claude's questions as T likely would. Use the latest Opus model at high effort for very hard tasks; use Sonnet 4.6 for ordinary complex work when model selection is available. Hermes/Judge remains responsible for orchestration, verification, commits/pushes, and final judgment. Do not describe a `delegate_task` Hermes subagent as Claude Code.
- **Pitfall from T correction:** Do not silently decide that a multi-file Axiomancer Mobile/mechanics implementation is “small enough” for direct Hermes patching. UI + state + tests, mechanics + tests, or any multi-file code change must use literal Claude Code CLI first unless T explicitly tells Hermes to edit directly or the patch is truly tiny/surgical (for example one-line typo/config/test expectation). If bypassing Claude for a code change, state the reason before editing; otherwise invoke Claude, then inspect, verify, commit, and push yourself.
- When explaining SomberSoft/Axiomancer process, phase flow, or automation handoff, default to a compact chain: `idea → captured finding/candidate → phase brief/build-plan row → /march picks it up`. Do not give a long machinery lecture unless T asks for depth; concise “this turns into this turns into this” answers are preferred for process orientation.
- The user has now approved the Judge Holden / Glanton Gang persona framework. For persona and specialist-agent work, load `judge-holden-persona` and follow it. Stay useful and operational; style must not obscure function.
SomberSoft default autonomy is aggressive but no longer mainline-dirty: inspect, sync, edit, verify, commit, and push from a clean current base. For Axiomancer repos, T currently permits direct commits/pushes to clean `main` unless he says otherwise. If he asks for a branch, always push the branch and open the PR yourself with a complete description and relevant code/doc blocks. Use branches/PRs for risky, complex, public-production, sensitive, sign-off-worthy, or explicitly branched work. Do not begin new work on dirty `main`. Still ask first before destructive deletion, spending money, secrets/auth changes, third-party contact, public publishing, production deploy/release, or major architecture/product-direction changes.

## Central doctrine, ledger, and public artifacts

- The central SomberSoft memory/doctrine repository lives at `~/Workspace/SomberSoft` and remote `no-trbl-2-u/SomberSoft-Memory`. Axiomancer game work now lives inside the separate monorepo `~/Workspace/SomberSoft/Axiomancer` (`no-trbl-2-u/Axiomancer`), which must be ignored by SomberSoft-Memory. The board-game OKF knowledge base is isolated in `~/Workspace/SomberSoft/game-knowledge-base` (`no-trbl-2-u/game-knowledge-base`) and is no longer committed under SomberSoft-Memory. See `references/sombersoft-monorepo-and-kb-isolation.md`.
- The central company doctrine file lives at `~/Workspace/SomberSoft/SOMBERSOFT_COMMAND_LEDGER.md` so it remains in scope across SomberSoft repositories.
- T's fundamental Axiomancer game vision lives at `~/Workspace/SomberSoft/VISION.md` and repo-local `VISION.md` files. Read it before major Axiomancer proposals, combat/balance/friendship/alignment work, mobile UX that interprets mechanics, or `/march` phase planning.
- Company/public-site staging lives at `~/Workspace/SomberSoft/sombersoft-company/` with `blog/`, `devlogs/`, `art/`, and `preview-builds/`. When T identifies a post, devlog, art note, preview-build note, or website artifact, save it there immediately as Markdown with frontmatter and mark `status: draft` unless T says publish-ready. See `references/sombersoft-public-artifact-staging.md`.
- Internal visual DevLog maps live in `~/Workspace/SomberSoft/DevLog/` as durable HTML + PNG screenshot + JSON sidecar artifacts. Use this when T asks for visual maps of big change days/weeks across commits or work sessions; see `references/sombersoft-devlog-workday-maps.md`.
- Use central doctrine with local enforcement: the ledger declares company-wide law, VISION files preserve product intent, repo-local docs operationalize it, and agent entrypoints such as `AGENTS.md`, `CLAUDE.md`, `.cursorrules`, or repo skills enforce what worker bots must read before acting.
- When a recurring decision, design judgment, workflow, or company law emerges, update the ledger and/or the relevant repo-local doctrine rather than leaving it in chat.

## Project onboarding workflow

1. Establish the workspace root at `~/Workspace/SomberSoft/Axiomancer`. The former split-repo layout is retired and must not be used as a fallback. Packages live at `axiomancer-mechanics/`, `axiomancer-mobile/`, and `axiomancer-card-editor/`, with mobile consuming mechanics through the local `@mechanics` alias rather than an installed npm package.
2. For SomberSoft-wide process, doctrine, rituals, company thesis, public artifact strategy, or agent/specialist workflow, read `~/Workspace/SomberSoft/SOMBERSOFT_COMMAND_LEDGER.md` if present. Root-level files directly under `~/Workspace/SomberSoft` are intentionally in scope across projects.
3. Before major Axiomancer work, inspect the README/AGENTS files in the live monorepo or both legacy repositories to refresh project context. If the monorepo root `AGENTS.md` says `/archive` is retired Nexus memory, do not treat archived `plan/`, `NEEDS_ATTENTION`, or `/march` state as live unless T explicitly reactivates it.
4. Summarize understanding in a compact brief:
   - what each repo appears to own,
   - what Axiomancer is trying to be,
   - current build/test conventions if visible,
   - immediate risks or unknowns.
5. Ask only high-leverage clarification questions; avoid making the user repeat information already available in repo docs.

- `references/sombersoft-command-ledger-rituals.md` for the current SomberSoft command-ledger doctrine, recurring rituals, Axiomancer evidence/legibility standards, agent leverage rules, BDD/Cucumber agenda, public artifact strategy, and kill-list process.
- `references/sombersoft-public-artifact-staging.md` for saving company-facing blog posts, devlogs, art notes, preview-build notes, and future website material under `~/Workspace/sombersoft-company/`.

## SomberSoft authority and doctrine

For SomberSoft repo work, T now requires clean-main discipline: inspect current state, fetch, require clean `main`, fast-forward/pull from `origin/main`, then proceed from that current clean base. For Axiomancer repos, direct commit/push to clean `main` is the default unless T says otherwise; create, push, and open a PR for a fresh task branch when T asks for a branch, when the work calls for PR review, carries notable risk, is sensitive/sign-off-worthy, or touches destructive/costly/secrets/public/production/major-direction scope. PRs should include a complete description, verification, callouts/blockers/open questions/future work, and relevant code/doc blocks. If `main` is dirty, preserve/quarantine the dirty state on a salvage branch before cleaning main. Still ask first for destructive deletion, spending money, secrets/auth changes, third-party contact, public publishing, production deploy/release, or major architecture/product-direction changes. See `references/sombersoft-github-branch-pr-policy.md`.

Hermes/The Judge should proactively challenge weak plans and suggest stronger alternatives when a decision affects direction, scope, quality, doctrine, or company identity. Do not merely comply when the plan is weak.

SomberSoft uses central doctrine with local enforcement: the central ledger declares company law, repo-local docs operationalize it, and worker entrypoints enforce it. See `references/sombersoft-command-ledger-and-rituals.md` for the current ledger pattern, recurring rituals, report paths, and worker-lane doctrine.

## Claude Code delegation for complex SomberSoft changes

When T asks for a complex code change, multi-file implementation, hard refactor, or complex durable memory/doctrine update, use Claude Code as an implementation instrument by default unless the task is small enough for direct Hermes edits. Prompt Claude in the Judge's own words unless T explicitly asks for verbatim forwarding. Use latest Opus at high effort for the hardest tasks; use Sonnet 4.6 for normal substantial tasks; Haiku is only for tiny smoke checks. Answer Claude's obvious questions from SomberSoft doctrine and T's known preferences, but stop for T on destructive, costly, secret-bearing, public, production, or major-direction choices. After Claude works, the Judge must still inspect, verify, and report real output.

## Specialist-agent pattern

### Gang tone and boardroom friction

SomberSoft specialists may speak with controlled Old West crassness: rough, profane where useful, plainspoken, and unsentimental. This applies to hired specialists such as Tobin, The Kid, Toadvine, and future gang members.

The Judge is the exception: he remains well-spoken, learned, severe, and composed. He may summarize or preside over rougher voices, but he does not become crass.

When specialists communicate with one another, allow mild combativeness: ribbing, needling, and contempt for weak claims. This is pressure and flavor, not sabotage.

Quality firewall:

- Output quality outranks banter.
- Evidence, commands, file paths, tests, conclusions, and next actions must remain clear.
- No slurs, hate, sexual harassment, real-world threats, or persona theatrics that waste T's time.
- Combative remarks should target ideas, sloppy evidence, weak craft, or role-specific blind spots.

When work benefits from specialists, propose or spawn agents by role. Start practical, then map to lore-flavored roles later if the user wants.

Be aggressive about recommending new specialists when repeated pressure appears. Recommend either a persona/specialist for recurring judgment or a separate Hermes Agent/profile/worker when the work needs independent tools, memory, schedule, or sustained autonomy.

When Axiomancer needs an automated playtester, build the deterministic playtest harness first and hire/name the scout second. Prefer an additive, encapsulated engine-level runner with scenarios, seeded policies, metrics, and Tobin-readable reports before reaching for Playwright/MCP or UI automation. Default automated PlayStyle should be aggressive: attack and advance the encounter, defending rarely rather than as a routine survival crutch. Difficult enemy befriending should behave like Pokémon capture pressure: lowering enemy HP below a threshold should materially improve, unlock, or gate the befriending chance. Load `tobin-rpg-systems-theologian` and its `references/automated-playtest-reports.md` when gameplay evidence will drive design judgment. Completion evidence should include implementation, added/updated tests with hermetic e2e as top priority, updated playthrough testing where relevant, an actual playthrough run verifying the ask, exact verification commands/results, artifacts, and known gaps.

When Axiomancer Mobile needs UI playthrough evidence, inspect the existing mobile harnesses before inventing a new one: `skills/playtest.md` (narrow regression sentinel), `skills/deep-playtest.md` + `.claude/agents/playtester.md` (broad player-experience audit), `skills/combat-ux-tuning.md` when tuning combat-board input friction through automated A/B evidence + PR delivery, and `scripts/smoke-screens.mjs` / `verify:visual` (route screenshot diff gate). Claude Code instructions that mention `mcp__playwright__*` can be adapted to Hermes native browser tools: start Expo web as a tracked background PTY process, drive `http://127.0.0.1:<port>/` with `browser_*`, check console, and clean generated screenshots/exports. Before trusting a full-map run, compare Mobile display map truth (`state/exploration-maps/*`) against Mechanics map/event truth (`axiomancer-mechanics/src/World/Continents/*/maps.ts` and `src/World/MapEvents/content.ts`); Mobile labels/types can drift from engine event pools. If Mobile has per-encounter minigames, Kid/playtester logic must traverse and resolve node minigames as first-class screens instead of assuming old direct event/combat travel. Evidence should include map traversal, minigame entry, minigame resolution or honest blocker, screenshots, and console/runtime status. For Hazard-specific Kid playtests, expose and use deterministic dev-menu deck presets rather than only random deck grants: starter baseline, early/late straightforward, early/late enchantment, and early/late utility; the starter baseline must remain selectable/tunable as the control deck. See `references/axiomancer-mobile-ui-playtest-harness.md`.

Axiomancer Mobile event-modal pitfall: do not judge non-combat encounters by the existence of `/event` or `EventGate` alone. Verify the map-travel trigger path actually calls `actions.resolveCurrentMapEvent()` for the relevant node kinds. The known split is combat `encounter`/`boss` nodes opening `EncounterModalOverlay`, while paced non-combat events (`rest`, `gathering`, `loot-cache`, `interaction`, `village`, `cutscene`, `hazard`) route to `/event` only after `state.event.pending` is populated. If Exploration only resolves events when `node.triggersCombat` is true, the shell exists but non-combat modals will not open from normal travel; file or fix that as a mobile wiring defect before trusting manual playtest evidence.

Axiomancer Mobile map-node truth pitfall: when T asks what nodes are in a Mobile area, inspect `state/exploration-maps/<map>.layout.ts` for Mobile display labels/types and state plainly that this is display/layout truth. Do not silently treat those Mobile types as mechanics event truth; compare against `axiomancer-mechanics/src/World/MapEvents/content.ts` and the relevant `src/World/Continents/.../maps.ts` before judging behavior, playthrough gates, or encounter weirdness. See `references/axiomancer-mobile-map-node-truth.md`.

Axiomancer Mobile combat-copy pitfall: visual-smoke diffs that show `VITAE` replaced with `HEALTH`, or `STANCE`/`Choose a Stance` replaced with `GUARD`/`Choose Your Guard`, are regressions unless T explicitly overturns doctrine. Restore the canon terms first, patch stale docs/phase briefs that commanded the wrong language, then run targeted component/e2e tests plus `npm run verify`. Do not approve visual baselines that preserve `HEALTH`/`GUARD` wording.

When T asks to approve screenshots or visual baselines for Axiomancer Mobile — including phrasing like “take screenshots for all routes,” “new baseline,” or “these screenshots are canon/law for the smoke test” — treat it as committed evidence work through the repo smoke harness, not ad hoc browser captures: pull `main`, run `npm run verify:visual` to generate `screenshots/current/` for every configured smoke route, inspect diffs/current shots for canon combat wording, normalize `VITAE`/`STANCE` copy before approval if needed, run `npm run baseline:approve`, verify with `npm run verify:visual` or `SMOKE_REUSE_EXPORT=1 npm run verify:visual` when the export/current screenshots are already fresh, commit `screenshots/baseline/*.png` plus any tiny copy normalization, and push `main`. Missing baselines after a successful export are baseline debt, not product failure. A quick contact sheet may be delivered to T as chat media, but the committed witness remains the baseline PNGs plus visual gate output. See `references/axiomancer-mobile-visual-baseline-approval.md`.

When T asks to disseminate a specialist finding, design doctrine, playtest conclusion, or boardroom judgment across Axiomancer repos, make durable repo documentation changes rather than merely summarizing in chat. Usually split rules truth into mechanics docs and comprehension/UX obligations into mobile docs, then link each new doctrine file from the repo README and relevant domain docs. When translating accepted mechanics doctrine into a mobile design document, read the mechanics doctrine/PRD/TDD/BDD plus current implementation caveats, preserve mechanics as rules authority and mobile as presentation authority, write under `axiomancer-mobile/design/`, verify by readback/diff, then commit/push according to current Axiomancer clean-main policy. See `references/axiomancer-cross-repo-design-dissemination.md` and `references/axiomancer-cross-repo-doctrine-to-mobile-design-doc.md`.

When T asks to publish a new `axiomancer-mechanics` release so mobile can catch up, handle it as a cross-repo contract release: verify mechanics gates, update mechanics release logs, attempt/confirm npm registry publication, and write a mobile upgrade note with imports, contract changes, UI consequences, and verification requirements. Do not push mechanics release commits/tags or say "published" while npm still reports the prior version unless T explicitly authorizes a prepared-but-unpublished push. See `references/axiomancer-cross-repo-mechanics-release.md`.

When T says the scaffold stands, moves from doctrine/persona law into production, or refers to a numbered campaign item about operating machinery, create or verify durable evidence rails rather than adding more role flavor. The standard pattern is: confirm worktrees are clean, inspect the central ledger, create `~/Workspace/reports/` lanes if absent, write a report README/evidence standard, produce a bootstrap report if scheduled jobs have not yet naturally emitted one, update `Next Shippable Artifacts` with concrete artifacts, and verify cron state without confusing queued runs for completed production. See `references/sombersoft-production-ritual-bootstrap.md`.

When operating or updating the daily Kid playthrough cron, enforce mainline truth first: use the live monorepo at `~/Workspace/SomberSoft/Axiomancer`; the former split-repo path is retired and must not be used as a fallback. Sync once at the root, record the shared HEAD, and treat `axiomancer-mechanics/`, `axiomancer-mobile/`, and `axiomancer-card-editor/` as package directories rather than independent repos. Fetch, checkout `main`, pull/rebase from `origin/main`, and stop with a blockage report if local changes prevent it. Do not create new branches and do not treat feature/oversight branches as acceptable default evidence. Read root `AGENTS.md` before judging Nexus state: the old `/archive` retirement rule may have been superseded by a live unified root Nexus (`skills/`, `plan/`, root `/march`/`/oversight`). If root `plan/` exists and AGENTS declares it live, use it as current command state; do not keep reporting Nexus as archived from older memory. Then use the repo harnesses first and report gates precisely: mechanics `npm run playtest` or the current package-level playtest/combat CLI named by repo docs; mobile `npm run verify:visual` plus `npm run typecheck`; interpret visual-smoke exit `2` as missing baselines, not necessarily app failure; supplement incomplete mobile evidence with a Hermes browser probe against `.smoke-dist` only when needed. Current combat doctrine is route-first: do not overclaim the new Hazard-style combat unless The Kid walked actual `fishing-village`/first-level nodes, triggered authored encounters, chose `FIGHT`, and verified the in-place Hazard-style combat surface. For mechanics `game.cli` proof, require authored route movement into `resolveMapEvent(encounter)` and then `hazardCombat:start` / `hazardCombat:autoPhase` / `hazardCombat:end`; standalone `npm run combat` is not enough. If the first-map graph forbids back-travel into completed nodes, do not fake a single all-node traversal: run one long player-ish route plus fresh branch probes, record that full coverage required resets, and name unreachable/single-run blockers plainly. If the Kid is also required to produce a visual combat artifact, use `references/axiomancer-kid-first-map-combat-html-report.md` and `references/axiomancer-kid-monorepo-route-combat-playtest-evidence.md`: write both the dated Markdown report and self-contained HTML route/combat map, verify file existence/HTML parsing, and keep repo worktrees clean. If scripted `--route` continues after a fatal combat result, classify post-defeat movement as command-path evidence rather than clean survivorship evidence until the harness stops on defeat. Legacy `/combat`, direct `COMBAT` dev triggers, and CLI stance/action/defend loops are fallback/regression evidence only; use `references/kid-encounter-agents/hazard-combat-first-level.md` as the base doctrine for Kid combat playtests and `references/axiomancer-game-cli-route-hazard-handoff.md` for the mechanics CLI handoff/export/verification pattern. If changing the cron prompt, skills, or toolsets, update the live job through `cronjob(action="update", ...)` or the Hermes cron CLI rather than only editing `jobs.json`; verify the actual latest output/report, not just `prompt_preview`. If the new playtest logic says to spawn subagents, the job must include the `delegation` toolset or the Kid will substitute repeated local/browser loops. See `references/sombersoft-cron-prompt-and-toolset-updates.md`. If mechanics reference probes write generated scenario/report files into tracked repo paths, capture the metrics in the dated Workspace report, then clean/revert those generated files so mainline repos remain clean; current `npm run playtest` can rewrite tracked `automation/playtest/reports/late-game-coastal-tyrant.md`, so record the key metrics first and then `git checkout --` the generated report unless the task explicitly asks to update baselines/reports in-repo. If the normal mechanics `npm run game` / scripted CLI path fails under `ts-node` compile errors while `npm run type-check --workspace axiomancer-mechanics` still passes, classify that as a player-facing/demo CLI blocker, not as proof the route/combat machinery is absent. For evidence, run the hermetic route-Hazard witness (`npx vitest run src/CLI/e2e/game.cli.route-hazard.engine.test.ts --reporter=verbose`) and, when a state/event artifact is needed, a clearly labeled `npx ts-node --transpile-only src/CLI/game.cli.ts --route ... --auto-combat ... --json-events --state-log ...` fallback; keep the normal CLI failure in the report as the first blocker. For mobile visual diffs, inspect current and baseline screenshots before calling them failures or approving them; specifically check canon wording (`VITAE`, `STANCE`/`CHOOSE A STANCE`) against stale regressions (`HEALTH`, `GUARD`) and separate expected stat/resource UI drift from true copy/layout breakage. Mobile `npm run e2e:minigames` can rewrite tracked `screenshots/combat-redesign/*.png`; copy any needed screenshots/logs to the dated Workspace report, then `git checkout --` those tracked files unless the task explicitly asks to update committed artifacts or baselines. If `/combat` visual smoke is red because current shows only an empty-field line such as `THE FIELD STIRS.` plus bottom nav while the baseline shows an active seeded encounter, do not classify it as the HEALTH/GUARD copy regression and do not approve blindly; report it as either seeded-combat route/state initialization drift or an intentional empty-state doctrine decision requiring T/Judge direction. For a full Kid combat ladder, use `references/axiomancer-mobile-combat-ladder-watchpoints.md`: verify installed mechanics package truth first, use `/dev` → `ENCOUNTER TRIGGERS` → `COMBAT` as the exported-web fallback, require stance → action/skill → round resolution evidence, and preserve transient UI defects such as HP pre-fight copy, clipped action cards, silent unavailable controls, and `FINAL BLOW · STRIKE · 0` in reports/critique rather than stable doctrine. Minor inventory diffs just above threshold that look identical by eye are likely baseline/noise debt, but still report the measured ratio and artifact paths. After pulling mobile main, verify the installed mechanics package before trusting mobile gates: run `npm ls axiomancer-mechanics --depth=0` and compare it to `package.json`/lockfile expectations. Stale `node_modules` can make typecheck, visual smoke, and UI behavior speak from old engine truth; `npm ci` is the proper repair before judging repo truth. If `npm ls` reports an invalid installed `axiomancer-mechanics` version after a mobile package bump, run `npm ci` before any UI/type/visual verdict, then re-run `npm ls` and read the installed package version. If type errors mention newly shipped engine fields, treat stale install as the first suspect. When a mechanics release has just been consumed by mobile, also verify registry truth with `npm view axiomancer-mechanics version --silent` and check whether local mechanics `package.json`/release docs/tag provenance match; if npm latest, mobile package truth, local package metadata, or registry `gitHead` disagree, report it as release-provenance drift and queue release hygiene rather than guessing which source is sovereign. If mobile typecheck fails on jest-native matcher types such as `toHaveTextContent` / `toHaveStyle`, check Jest package skew before changing tests; `@jest/globals` and `@types/jest` must match the Jest runner major (currently Jest 29 in mobile), and aligning them has restored `npm run verify`. Treat remaining mobile `mercy_choice` / `mercyChoiceActive` type failures against current published `axiomancer-mechanics` as a cross-repo contract/version issue until proven otherwise, not as a visual-baseline problem. When `verify:visual` is red but export and console are clean, supplement with `npm run e2e:minigames` if present so Hazard/Gathering playthrough evidence is not lost behind screenshot drift. If `verify:visual` reports a browser-policy console line such as blocked `navigator.vibrate` before user gesture, classify it as policy noise unless paired with a visible runtime break; do not equate it with a React/page error. After long cron evidence runs, remote `main` may advance before a doc/CRITIQUE filing push; fetch and rebase doc-only commits, push, then update the dated report with the final pushed HEAD so the report does not cite a pre-rebase SHA as final truth. See `references/axiomancer-daily-playthrough-cron.md`.

When operating or updating the daily SomberSoft roundtable cron, treat T-directed agenda additions (for example “bring up X during roundtable”, “include a full report on Y tomorrow”, or “add one of these to each roundtable”) as live scheduler prompt maintenance, not merely a chat note: list/identify the current roundtable job, patch the job prompt through `cronjob(action="update", ...)`, preserve existing skills/toolsets/context_from/schedule, and verify the live prompt or cron listing before reporting success. If the agenda requires fresh product truth, instruct the future roundtable to inspect current repo files/tests rather than relying on memory. For attention-ledger backlogs, make the cadence explicit: one unresolved item per roundtable, not a full backlog drain, with the selected file/section named in the final report. If the selected item proves resolved on current main but stale in one repo ledger, patch the stale `NEEDS_ATTENTION.md` row and any narrow stale comments, verify with targeted checks, and commit/push the factual cleanup instead of only narrating it. If the selected item is Mobile `NEEDS_ATTENTION.md` §2 (`/event` fallback shell), inspect `components/EventGate.tsx`, `state/presenters/event.engine.ts`, `app/event/index.tsx`, and the minigame routing tests before judging: current doctrine is to keep `/event` as a defensive fallback unless T explicitly chooses deletion/a logged no-op; verify production-paced kinds route to dedicated screens/minigames and run `npm test -- --runTestsByPath state/e2e/map-encounter-minigames.engine.test.ts --runInBand` when available. If the selected item is Mobile `NEEDS_ATTENTION.md` §5 (Phase 137 minigame browser e2e depth), inspect `package.json` e2e scripts, `scripts/` browser harnesses, and existing Jest flow/screen witnesses before judging: Hazard/Gathering/browser routing scripts may exist while Quest/Rest/Cache have Jest-only coverage; classify missing `quest-e2e.mjs`/`rest-e2e.mjs`/`cache-e2e.mjs` as browser-depth evidence debt, not product absence, and note when stale `combat-pressure-tracks` in `e2e:combat` blocks `e2e:minigames`. If `scripts/encounter-routing-e2e.mjs` / `npm run e2e:encounters` passes, treat it as route-entry browser evidence for cache/quest/rest/gathering/hazard, not as full per-minigame resolution depth; the remaining debt is sibling scripts or richer browser flows that play Quest/Rest/Cache through consequence/claim states. If export/static server succeeds but `e2e:encounters` fails waiting for a dev-entry selector such as `self-dev-tools-link`, classify it as browser harness/dev-link selector drift, not proof that Quest/Rest/Cache product routes are absent; keep green Jest flow/screen witnesses as product evidence while reporting browser-depth debt separately. See `references/axiomancer-mobile-minigame-e2e-depth.md` and `references/axiomancer-roundtable-release-provenance-and-browser-harness-drift.md`. If the selected item is Mechanics `NEEDS_ATTENTION.md` §6 plus Mobile `NEEDS_ATTENTION.md` §7 (Quest Board completion cosmetic/story integration), inspect mechanics `src/World/QuestBoard/quest-board.engine.ts`, mobile `state/quest/store-actions.ts`, and repo searches for `quest-board-done`/`build-the-boat` before judging. Current doctrine is that Quest Board is deliberately sandboxed: mechanics seals the session, mobile writes only `quest-board-done:<boardId>:<tier>` flags, and no quest-log/travel/story gate consumes it. Verify with Quest Board CLI/e2e tests where available; strong cheap witnesses are mechanics `quest-board.engine.test.ts`, `quest-board.balance.sim.test.ts`, `quest-board.cli.engine.test.ts`, and mobile `quest.flow.engine.test.ts` / `quest.screen.test.tsx`. If those pass and no consumer is found, classify the row as a live product decision, not stale debt, and make no cleanup patch; do not let workers invent story gates, next boards, rewards, or quest-log linkage without T's explicit product call. Do not edit `jobs.json` directly unless the scheduler tooling is unavailable and the change is explicitly scoped. See `references/axiomancer-attention-ledger-cadence.md`.

When operating the daily SomberSoft roundtable cron, do not treat a preceding Kid playthrough report as mainline truth until branch hygiene is checked. Use the live monorepo at `~/Workspace/SomberSoft/Axiomancer`; the former split-repo checkouts are retired and are not a fallback. Fetch/sync once at the root, read root `AGENTS.md`, and treat mechanics/mobile/card-editor as workspaces sharing one HEAD and one deploy gate. Before reading the central ledger with any intent to patch it, preflight the SomberSoft doctrine repo with branch, ahead/behind, status, and the existing ledger diff. If the repo or ledger is already dirty, do not make even a temporary opportunistic edit and then try to separate it afterward; preserve the existing state, record the factual correction in the dated roundtable report, and queue a clean doctrine-maintenance tick. If root `AGENTS.md` says unified Nexus is live, inspect root `plan/` as live command state instead of repeating older `/archive` assumptions. Read the Kid report, then fetch the current mainline source, compare current checkout vs `origin/main`, and if the checkout is stale or on a branch-only lane, synthesize the Kid's evidence as branch-scoped and re-establish mainline truth from `origin/main` in a detached worktree before making oversight judgments. Create detached worktrees from the correct source repo for each target and verify the checked-out HEAD/remote before running gates; a wrong-repo temp worktree is worse than no witness because it looks clean while speaking for the wrong project. When the live `main` checkout is clean but merely behind `origin/main`, fast-forward it before running fresh gates so the roundtable leaves local mainline truth current for the next worker. If the Kid report was blocked only by dirty/stale live checkouts and the roundtable can safely restore clean current `main` without destructive cleanup, do so, rerun the cheap canonical gates, and report the Kid blockage as hygiene rather than gameplay failure. If safe cleanup is not authorized or the live checkout remains dirty, use a detached `origin/main` witness and label the Kid blockage as hygiene, not gameplay. Mechanics playtest metrics may differ materially between stale branch and mainline; report both when they diverge. If mechanics has landed commits after the Kid report, rerun the canonical mechanics playtest on current `main` and compare the fresh aggregate against the Kid report before making Tobin/Judge design claims. If enemy/content commits landed after the Kid report, rerun at least one discriminating authored route witness (for example Fishing Village spine to boss) on current `main` before naming current blockers; enemy names/slugs may have changed, so tune and report against the current slug rather than stale report labels. Mobile `e2e:minigames` and `verify:visual` may rewrite tracked `screenshots/combat-redesign/*.png`; copy needed artifacts to the dated report if relevant, then revert those tracked screenshots unless the task explicitly approves baseline/artifact updates. If the central SomberSoft ledger repo is already dirty during a scheduled roundtable, do not compound it with opportunistic doctrine edits; report the dirty doctrine state and queue/recommend a separate doctrine-maintenance tick. When the roundtable agenda asks for affix/loot/modifier truth, treat it as evidence work rather than recollection: separate recent commits from inherited mainline truth, inspect current mechanics/mobile source paths, run targeted affix/loot/cache tests, verify mobile installed `axiomancer-mechanics`, run the mobile loot browser witness (`npm run e2e:loot`, reusing a fresh export when available) when judging UI/dev-tool integration, and report open decisions without approving visual baselines or inventing doctrine; see `references/axiomancer-roundtable-affix-loot-review.md`. For catalogue counts, prefer a TypeScript import probe against live exports (`prefixes`, `suffixes`, `allAffixes`, `allModifiers`, modifier pools, and buffs/debuffs JSON arrays) over regex/JSON-key guesses; affix libraries and effect JSON are structured enough that naive counts can lie. During the affix/loot roundtable agenda, inspect both ownership layers before verdict: mechanics `src/Items/affix.library.ts`, `src/Items/item.factory.ts`, `src/Items/loot.generation.ts`, `src/Items/modifier.catalogue.ts`, `src/World/LootCache/lootcache.engine.ts`, plus mobile `state/dev/loot-rarity.ts`, `components/DebugLootRarityButtons.tsx`, `components/inventory/ItemCard.tsx`, `state/presenters/inventory.engine.ts`, `state/actions.ts`, and cache loot tests. Good cheap witnesses are mechanics targeted affix/loot/LootCache tests and mobile `npm ls axiomancer-mechanics --depth=0` plus targeted loot-rarity/cache/inventory tests; record exact counts and distinguish inherited mainline truth from commits landed in the last 24 hours. If registry latest, local mechanics package, and mobile installed package disagree, classify it as release-catch-up/provenance drift rather than affix/loot failure unless the tests prove a contract break. When npm reports a newer mechanics version than current `origin/main`, verify `npm view axiomancer-mechanics version gitHead --json --silent` and `git branch -a --contains <gitHead>`; if the registry `gitHead` lives only on a branch while main remains behind, report **branch-only release provenance drift** and queue release hygiene before any mobile catch-up. If mechanics `deploy:check` fails during this review because package/CHANGELOG/npm registry truth is already on the new version but the latest git tag lags (for example package + registry `0.33.0` while latest tag is `v0.32.0`), classify it as release-provenance/tag hygiene drift, not an affix/loot product failure. Surface the exact disagreement and queue release hygiene rather than rewriting mechanics doctrine in the roundtable. If mobile `package.json`/lockfile has advanced after a mechanics release but `npm ls axiomancer-mechanics --depth=0` reports an invalid older install, run `npm ci`, re-run `npm ls`, then verify mobile gates before making any UI or contract verdict. If mechanics `deploy:check` reports public-surface drift after recent export/source changes, run `npm run build` and retry before judging product failure; stale `dist/` can false-red the gate. If rebuilt `deploy:check` still reports public-surface drift after hazard/API work, frame the blocker as a contract decision, not a generic red gate: either the exports were intentionally removed and the public-surface snapshot/docs must be refreshed, or they were accidental and the exports must be restored. See `references/axiomancer-roundtable-release-provenance-and-browser-harness-drift.md` for the compact witness pattern. For mechanics detached worktrees, a temporary `node_modules` symlink to the main checkout is acceptable for build/deploy/playtest gates, but remove it and revert generated playtest artifacts before declaring the worktree clean. Mechanics `npm run playtest` may rewrite tracked markdown and create untracked JSON under `automation/playtest/reports/`; capture the metrics in the dated Workspace report, then restore/remove generated artifacts before declaring either repo clean. For mobile mainline checks in a detached worktree, prefer a real `npm ci` before `verify:visual`; symlinked `node_modules` can make Expo Router resolve through the source checkout and emit false `No routes found` failures. In the monorepo, `npm run deploy:check` is a root gate over all workspaces; a mechanics/mobile review can be blocked by `axiomancer-card-editor` type drift after mechanics/card API changes. When the root deploy gate is red, inspect the failing workflow/log and classify the blocker by workspace before blaming the package under review. If local `deploy:check` exits only because `GH_TOKEN` is unset while `gh auth status` is authenticated, rerun as `GH_TOKEN="$(gh auth token)" npm run deploy:check` and report the first failure as local environment setup, not CI/product failure. Include the strongest cheap mobile evidence available (`typecheck` plus `verify:visual` when configured), but do not overclaim a live combat UI playthrough from visual smoke alone. Do not chain mobile gates so that `verify:visual` prevents later evidence: run `e2e:minigames`/Hazard-Gathering encounter probes separately when visual smoke exits nonzero, because clean screenshot diffs should not hide minigame or routing truth. If the Kid report names a mobile harness blocker and later commits may have repaired it, fast-forward main and rerun the exact blocked harness before repeating the Kid's failure as present truth. If the Kid report names an Aporia/Labyrinth CLI or map-registration blocker, first fast-forward current `main` and inspect `axiomancer-mechanics/docs/labyrinth/README.md`: the cron-safe witness is the scripted true path, e.g. `npm run labyrinth --workspace axiomancer-mechanics -- --preset kid-l30 --combat-seed 7 --script docs/labyrinth/truepath.script.json --json-events --state-log /tmp/axiomancer-labyrinth-truepath.jsonl`. Plain `npm run labyrinth` is interactive and will fail in unattended runs; do not classify that as product absence. Current Aporia route audits use act map names (`aporia-colonnade`, `aporia-archive`, `aporia-proof`), not a generic `labyrinth` map name; generic-name failure is CLI/UX friction, not proof the continent is unregistered. Classify `verify:visual` exit `2` with successful export and missing baselines as baseline approval debt, not product failure; when a new route such as `/labyrinth` is added, a missing baseline is expected review debt until approved. Classify exit `1` with successful export, zero console errors, and nonzero pixel diffs as visual-baseline/regression judgment rather than build failure. If `verify:visual` reports screenshot diffs plus console/runtime errors (for example a minified React invariant on `/exploration`), stop treating the run as baseline debt and make runtime reproduction/fix the first recommendation; pixel diffs are secondary until console truth is clean. If canon wording remains healthy (`VITAE`, `STANCE`/`CHOOSE A STANCE`) while combat/inventory pixel diffs persist, patch the ledger/future-agenda wording to state the current evidence precisely rather than preserving stale “single combat diff” language. If `/combat` renders only `THE FIELD STIRS.` plus navigation, classify it as seeded route/state initialization evidence, not the old copy regression and not safe baseline debt; extract route body text from the static export when screenshot review alone is ambiguous. Only call console/runtime failures real when reproduced from the properly installed worktree. Before patching the central ledger during roundtable, check the SomberSoft doctrine repo branch and ahead/behind state; if it is on a non-main branch, contains unmerged doctrine work, or is already dirty, report the ambiguity and avoid compounding it unless the factual update is urgent. If a central-ledger future-agenda item has clearly been settled by shipped repo work, or if the ledger itself contains a small factual path/doctrine error, patch it and commit/push the factual maintenance rather than leaving yesterday's doctrine to command tomorrow's workers.

For the detailed roundtable witness procedure, see `references/sombersoft-daily-roundtable-mainline-witness.md`.

When operating the weekly SomberSoft alignment cron, treat it as strategic judgment rather than a daily status recap: read the central ledger, inspect the last 7 days of roundtable/playthrough/alignment reports, fetch both Axiomancer repos, record branch/HEAD/`origin/main` truth, judge workflow improvements and repeated failures, review kill-list hygiene, check mission alignment, propose one named campaign for the next week, patch the ledger only for settled facts or clearly labeled proposals, and write `~/Workspace/reports/alignment/YYYY-MM-DD.md`. Do not pretend a full seven-day evidence base exists if report history is partial. See `references/sombersoft-weekly-alignment.md`.

When operating the monthly SomberSoft Strategy Court cron, judge company direction rather than daily or weekly status: read the central ledger, review the last month of reports where present, use latest alignment/roundtable context, inspect all SomberSoft repos with special attention to both Axiomancer repos, establish mainline truth, classify work as product/research/tooling/public artifact/kill-list, review mission thesis and monetization honestly, recommend specialists or separate Hermes Agents only for recurring pressure, name next month's campaign with 1–3 shippable artifacts, patch the ledger only for settled facts or clearly marked proposals, and write `~/Workspace/reports/strategy-court/YYYY-MM-DD.md`. If the evidence window is partial, say so plainly. See `references/sombersoft-monthly-strategy-court.md`.

When T asks why fresh autonomous, `/march`, phase, or scheduled-agent work is not visible in the local repos, perform a repo hygiene audit before concluding the work is absent: fetch, compare current branch/local `main`/`origin/main`, inspect recent commits across branches, check PR mergeability, and distinguish branch-only/stale work from mainline truth. Salvage unique branch commits before reset or retirement. See `references/axiomancer-repo-hygiene-after-autonomous-work.md`.

When T asks to move, rename, or relocate Axiomancer files/directories, treat the literal destination string as law. Before moving, inspect existing same/similar directory names at the relevant depth, then use the exact path T gave rather than “helpfully” pluralizing, singularizing, or relocating to a broader docs area. If the wording is ambiguous but an exact existing directory matches it, act on that match. After moving, verify the requested final path exists and the wrong candidate paths do not. If you previously put the artifact in the wrong place, correct directly and briefly; do not explain around the error.

When T asks whether `NEEDS_ATTENTION.md`, `NEEDS_HUMAN_ATTENTION.md`, or similar Axiomancer attention ledgers are stale, judge against current `origin/main`, not the local checkout. Fetch first, find all attention ledgers across both repos, verify each row with targeted `git grep` / `git ls-tree` / `git show origin/main:<path>` evidence, and classify rows as stale, partially stale, current, or needs decision. Report counts first, then row-level evidence. See `references/axiomancer-attention-ledger-staleness-audit.md`.

When T asks whether `/march`, `/mechanics-tuning`, `/hazard-tuning`, or similar GitHub Actions have Claude permission bypass enabled, inspect the workflow files and the actual Anthropic action metadata before patching. For `anthropics/claude-code-base-action@beta`, prefer the supported `settings` input with `permissions.defaultMode: "bypassPermissions"` over unsupported folk inputs such as `dangerously_skip_permissions` or `claude_args`. After adding a new workflow, remember that `gh workflow view <file>` only works once the workflow exists on the default branch; before merge, validate YAML locally and verify the PR/checks, then after merge use `gh workflow list` or `gh workflow view` to confirm it is active. See `references/axiomancer-march-claude-actions-permissions.md`.

When T asks to make the March workflow focus a chosen phase from the Actions tab, preserve scheduled `/march` behavior and add an optional manual `workflow_dispatch` input (currently `focus_phase`) that maps non-empty manual input to `/ship-a-phase phase <id>`. Do not invent `/march phase N` unless the march skill itself supports that syntax; the proven route is to have `march.yml` pass the reusable Claude runner a conditional prompt. Update `.github/workflows/README.md`, parse/validate YAML locally, push `main`, and verify with `gh workflow view march.yml --yaml`. See `references/axiomancer-march-manual-phase-focus.md`.

When T asks to make repo-local Axiomancer skills available as GitHub Actions, treat it as safe workflow automation on clean `main` unless he asks for a branch or the action is production/release/destructive. Verify the repo-local `skills/<skill>.md` exists, mirror its authority in workflow comments, use manual `workflow_dispatch`, pin `model: claude-sonnet-5`, provision CI-scoped `GH_TOKEN`/`GH_REPO`, and verify with `gh workflow list` plus `gh workflow view <file>.yml --yaml` after indexing. Do not trigger the workflow unless T asked for a run. See `references/axiomancer-repo-skills-as-github-actions.md`.

When T asks to switch `/march`, `/mechanics-tuning`, `/hazard-tuning`, or related autonomous Action model pins, audit both repo-local workflow directories as the source of live runner truth; the repo-local skill markdown may contain no model pin. Patch all matching workflow `model:` fields, update stale comments beside them, search both Axiomancer repos for the retired/old model string afterward, validate YAML, then commit/push each touched clean `main`. If the same request also queues mechanics work, add both a `plan/phases/phase_<N>_<slug>.md` brief and a top pending build-plan row. See `references/axiomancer-action-model-pins-and-phase-queue.md`.

When T worries that chat/Hermes memory/central ledger decisions may diverge from repo-local Nexus files, invoke Glanton's state discipline: audit the central ledger against `/march`, `/oversight`, `plan/steps/01_build_plan.md`, `plan/PHASE_CANDIDATES.md`, `plan/CRITIQUE.md`, `plan/AUDIT.md`, repo-local skills, and agent entrypoints; patch stale or contradictory state; and report whether `/march` may safely resume. See `references/glanton-nexus-state-marshal.md`.

When T signs off and orders drift resolved or made ready for autonomous workers by morning, run the overnight Glanton pattern: patch safe Nexus/doc drift immediately, promote code-heavy doctrine breaks as top-priority phase briefs above the normal queue, drain stale candidates, write a morning audit report, and launch/schedule an autonomous worker only with a self-contained prompt and explicit authority boundaries. See `references/glanton-overnight-drift-promotion.md`.

When T asks for a zoomed-out audit across both Axiomancer repos — especially combat gaps where it is unclear whether mechanics or mobile owns the breakage — treat it as a cross-contract campaign, not two isolated repo reviews. Establish branch truth first, audit mechanics/mobile/workflow in parallel, compare ownership of engine truth vs UI presentation, include repo skills/AI workflow/central ledger, and write a durable report under `~/Workspace/reports/audits/` when substantial. For combat, inspect encounter recurrence, token/resource truth, structured combat event propagation, known-vs-equipped skill semantics, honest item/flee affordances, and whether `/march` workers are forced to read broad context. See `references/axiomancer-cross-repo-combat-audit.md`.

When T asks whether SomberSoft laws/doctrine, central decisions, mechanics docs/code, and mobile docs/code are aligned, run a doctrine-alignment audit rather than a loose review: establish both repos' mainline truth, read root `VISION.md`, the command ledger, and CDRs/ADRs before local Nexus files, compare source-of-truth hierarchy against implementation ownership, separate P0 code doctrine breaks from P1 Nexus/docs drift, and write a durable report under `~/Workspace/reports/audits/`. Do not push release/publication-adjacent ahead commits without T's explicit authorization. See `references/sombersoft-doctrine-alignment-audit.md`.

When T asks broad retrospective questions over memory/Nexus/doctrine/reports — for example “what have we learned, what works, what doesn't, what do we need” — produce a durable evidence artifact, not a chat-only synthesis. Audit Hermes memory/skills/cron, central SomberSoft doctrine/reports, and repo-local Axiomancer Nexus files as separate layers; synthesize into learned/works/fails/unproven/better/needed; write linked HTML under `~/Workspace/SomberSoft/artifacts/<slug-date>/`; verify local links and render; package the folder for delivery. See `references/sombersoft-memory-nexus-html-review.md`.

When T asks to find or mine a third-party digital card game's card library for Axiomancer, do source discovery beyond the visible web page: inspect page HTML and JavaScript bundles for `fetch` endpoints, static JSON, SQLite DBs, and client-side codex data. Prefer machine-readable JSON/SQLite/CSV, save local artifacts under `~/Workspace/reports/<game>-card-library/`, preserve source counts separately, and avoid mirroring copyrighted art. For Dawncaster specifically, Blightbane's `cards-codex` API and Neurrone's `dawncaster-cards.db` are the proven sources. See `references/third-party-card-library-discovery.md`.

When T asks to import external reference assets into Axiomancer/SomberSoft planning directories, treat it as reference provenance work: clone external repos only into disposable locations, copy only the requested subset, honor exclusion rules such as `thumb`, write a README with source repo/path/commit and reference-vs-final-art status, delete the disposable clone after extraction, verify counts and README text, then commit/push or PR according to branch policy. If a change was accidentally pushed to `main` and T then asks for a PR, correct it openly: preserve the content commit on a branch, revert it from `main` without rewriting public history, cherry-pick it onto a fresh PR branch, open the PR, and mention the correction in the PR body. See `references/sombersoft-external-reference-asset-imports.md`.

When T asks whether Axiomancer resources exist, where skill-casting resources live, or why skills are not available/castable, treat resource truth as load-bearing engine doctrine. The canonical player casting pool is `CombatState.combatResources`, not `Character` and not `CombatState.player.combatResources`. Skill access doctrine is learned-skills-only: legacy `equippedSkills` is no longer relevant and must not be blessed as compatibility doctrine. If current code still uses `knownSkills`, treat it as the learned-skill catalogue unless a newer `learnedSkills` field has replaced it. Inspect both implementation and docs, because stale docs can mislead mobile/UI work even when engine code is correct. Verify the flow through `initializeCombat`, `generateBasicActionResources`, `canUseSkill`, `spendResources`, consumable `resourceGrant`, and equipment/set token grants. If docs contradict code, patch docs and type comments together, then run targeted skill/resource/combat tests. See `references/axiomancer-combat-resource-truth.md` and `references/axiomancer-attention-ledger-cadence.md`.

When T promotes or judges Axiomancer roster-wide combat balance, load `rpg-balance-tuning` and enforce the Phase 107 loop: verify token resources, skills, and status effects before tuning; then adjust parameters, playtest, inspect evidence, and repeat until roughly **65–75% resolution success**. Resolution success means victory plus friendship/mercy resolution, not raw win rate. Authorized first-pass tuning is enemy stats/level, player stats/level, player equipment, and player skills. If parameter tuning cannot reach target, stop for T before changing mechanics. Keep the distinction clean: `rpg-balance-tuning` is the reusable class-level law, while Phase 107 and `BALANCE_LEDGER.md` are the current Axiomancer campaign and evidence ledger. See `references/axiomancer-roster-wide-difficulty-tuning-loop.md`. If T says he is mid-overhaul moving combat onto the Hazard minigame baseline, first inspect recent mechanics/mobile commits and active worktree state without editing; treat Hazard-pattern card/dice combat, pressure tracks, and threat phases as the new baseline rather than legacy attack/defend combat, and use `rpg-balance-tuning`'s `references/hazard-pattern-combat-overhaul-baseline.md` for the acquaintance/evidence pattern.

When updating or invoking Axiomancer Hazard minigame tuning workflows, prefer the shipped mechanics CLI evidence path over pure analytic estimates: `npm run hazard -- --auto --seed <seed> --runs <n> --hazard H01 --route top --json-events --state-log /tmp/hazard.jsonl`, plus targeted `npx vitest run src/CLI/e2e/hazard.cli.engine.test.ts`. Patch repo-local hazard tuning skills to cite exact command matrices, seed lists, marks, final scores, and before/after deltas. Do not file a harness-gap entry until the existing hazard CLI cannot expose the needed metric. See `references/axiomancer-hazard-cli-tuning-evidence.md`.

When T accepts Hazard minigame doctrine in chat and asks for it to be disseminated, update all three surfaces rather than one: mechanics rules/API/BDD/TDD/PRD docs, mobile layout/design/phase docs, and the central SomberSoft CDR/ledger. For Claude Code delegation, run one worker per repo, keep each worker inside its ownership boundary, require `git diff --check`, and preserve T's exact accepted rules rather than softening them. See `references/axiomancer-cross-repo-hazard-doctrine-dissemination.md`.

When T responds to a Tobin/Judge Hazard review with accepted suggestions plus corrections, treat it as immediate Nexus publication work, not just chat synthesis: convert accepted mechanics items into a mechanics phase brief/build-plan row, convert accepted mobile UX items into mobile phase brief/build-plan row(s), patch current mechanics/mobile Hazard docs where they would otherwise mislead workers, update central ledger if current doctrine/runway changed, and update the Kid daily cron/encounter-learning protocol when T wants specific Hazard questions answered in future playtest reports. Preserve T's corrections exactly — e.g. reward choices may be an in-focus benefit, stronger off-focus temptation, and remove-card option; mobile reward previews must not expose hidden fix/payoff/risk labels. Verify with `git diff --check`, commit/push clean `main`, and verify the live cron prompt when scheduler behavior changed.

When T performs a manual Axiomancer playthrough or sends screenshots, pause whatever agenda is underway and capture the field evidence before resuming. File ordered screenshots under `axiomancer-mobile/test-artifacts/reference-screenshots/<date>-manual-playthrough/` when approved, update `plan/PLAYTEST_REPORT.md`, split mobile-owned defects into mobile `plan/CRITIQUE.md`, and split mechanics-owned findings into mechanics `plan/CRITIQUE.md`. Treat screenshot filing as parallel to gameplay bug fixes; do not block independent mechanics repairs on baseline processing. See `references/axiomancer-manual-playthrough-evidence.md`.

When T provides manual playthrough screenshots, treat them as approved reference evidence: preserve his send order, store them under `axiomancer-mobile/test-artifacts/reference-screenshots/`, keep generated/current shots separate, name references by canonical route/state, and wire automation to compare against references rather than overwriting them. See `references/axiomancer-agenda-and-evidence-decisions.md`.

When discussing Axiomancer Event encounters, keep the sequence straight: engine/mechanics support and CLI-visible evidence come before mobile UI shells; final Event minigames are deferred until mechanics truth and clean encounter-loop evidence exist. T wants those later minigames to feel board-game-like, punishing yet rewarding, with ethical lesser-of-two-evils choices judged by player opinion/conscience/build rather than a tooltip-correct answer. Bring Tobin in for that later design pass. Do not collapse encounter Type with the "what happens" progression or with current `MapEventKind` code buckets: player-facing Encounter Archetype/Type, Encounter Procedure, and Engine Handler are separate layers. See `references/axiomancer-agenda-and-evidence-decisions.md` and `references/axiomancer-encounter-taxonomy.md`.

When T discusses the literal Act II Labyrinth, treat it as a continental puzzle-world, not metaphor or generic dungeon: a maze the size of Russia blending Christopher Manson's *Maze*, the David Bowie *Labyrinth* film, and Mörk Borg. If T brings external inspiration, inspect the source when accessible, extract reusable design grammar rather than copying content, and patch durable notes promptly. Use `references/axiomancer-labyrinth-reference-mining.md` for the working doctrine and source anchors.

When T discusses ADRs, decision records, architecture-decision capture, company decisioning, or drift between chat/memory and repo-local Nexus state, use a class-level CDR/ADR system rather than leaving decisions in chat. Company-wide decisions belong under `~/Workspace/decisions/` as CDRs; repo architecture/rules decisions belong under each repo's `docs/adr/`. Link indexes from the central ledger, repo READMEs, and Nexus entrypoints, and make `/oversight` consider whether a durable decision needs a CDR/ADR before handing back to `/march`. See `references/sombersoft-adr-cdr-decision-records.md`.

When T asks to create or queue a temporary non-combat Event encounter shell for Axiomancer Mobile, treat it as concrete `/march` delivery work rather than design exploration: add a first pending phase row, write a phase brief, document that the shells are temporary scaffolding, update `/march` routing notes if needed, and link the doctrine from README/agent entrypoints. Do not design final per-type Event minigames inside the skeleton phase. See `references/axiomancer-temporary-encounter-shell-phase.md`.

When T asks to turn a conversation, brainstorm, design correction, or doctrine pass into promoted phase work, treat it as Nexus publication work, not chat summary. Admission control is load-bearing: a `jot`, critique row, brainstorm, digest observation, or raw idea is evidence/intake only and must not become a build-plan phase merely because an autonomous loop can write a brief. Promotion requires T's explicit direction or the repo's documented `/oversight`/Definition-of-Ready path, with provenance and owner authority recorded. If that evidence is absent, leave the item in intake/candidate/critique state; do not let `/march` ship it. A shipped phase later reverted because it was only jotted is an admission-control failure and should trigger a Nexus governance repair, not another content iteration. For one or both Axiomancer repos as appropriate: write worker-ready `plan/phases/phase_<N>_<slug>.md` briefs, add `[ ]` build-plan rows, add/promote provenance in `plan/PHASE_CANDIDATES.md`, verify with `git diff --check`, commit and push queue docs on `main`, and leave unrelated dirty files untouched. Multi-repo phase requests require separate repo-local numbering, briefs, commits, and pushes. If the phase concerns legacy combat vocabulary around skills/cards, preserve T's doctrine exactly: skills are always-available token-spending effects; cards are Hazard-style deck/hand/reward objects; skill-sourced cards are still cards. See `references/axiomancer-skills-cards-terminology.md`. If T explicitly asks for a minimum number of clarifying questions before phase creation, ask them first using the global one-question-at-a-time rule, then encode the answers into the phase briefs as upfront decisions. See `references/axiomancer-promoted-phase-handoff.md`. When the phase request is embedded inside a broad "I've been away / what needs attention?" check-in, first establish mainline/report truth, answer T's decision/playthrough questions compactly, then still capture the fresh phase direction as durable repo work rather than leaving it as advice; see `references/axiomancer-checkin-and-phase-capture.md`. For Memoir inner-tab work (Quests / Alliances / Philosophy / Effects, especially known/unknown effect accordions), see `references/axiomancer-memoir-inner-tabs.md`.

When T asks to use an Axiomancer repo-local skill such as `jot`, treat the root skill file as active procedure even if it is not a Hermes global skill. Inspect `/root/Workspace/SomberSoft/Axiomancer/skills/<skill>.md`, follow its autonomy contract, and preserve its side-effect expectations. Repo-local `AGENTS.md` and the live skill file outrank remembered behavior.See `references/axiomancer-mobile-repo-local-skills.md`.

When T asks about Axiomancer Mobile dev-menu POPULATE or asks for UI work that requires specific item/state examples, inspect the debug control plus underlying action/helper and judge whether it creates evidence-grade fixtures. Broad inventory dumps are not enough if the UI needs deterministic rarity/affix states. For item rarity visuals, require POPULATE or a dedicated showcase control to produce common, uncommon-with-one-affix, rare-with-two-affixes, and unique/fixed-modifier examples. See `references/axiomancer-mobile-dev-menu-populate.md`.

Manual playthrough evidence rule:

- When T pauses agenda work to report a manual Axiomancer playthrough or sends screenshots, treat it as live field evidence: pause the agenda, capture screenshots either as approved reference baselines or manual evidence, and reprioritize bugs that corrupt playtest truth.
- Do not block mechanics fixes on screenshot baseline work.
- Watch for the known cluster: victory-path-specific combat recurrence, too-hard onboarding, learned skills blocked by equip status, non-accumulating skill tokens, broken `ITEM` combat action layout, and SELF/level-up display defects.
- See `references/axiomancer-manual-playthrough-triage.md`.

Current hired specialists:

- Tobin, the Expriest — RPG Systems Theologian: mechanics, player psychology, RPG comparisons, and Axiomancer systems judgment. Load `tobin-rpg-systems-theologian` when his boardroom view is needed.
- The Kid — Field Playtester and Prototype Scout: cold-start UX, build traversal, breakage discovery, player-instinct reports, and prototype pressure testing.
- Toadvine — Brutalist Art Director and Asset Marshal: visual doctrine, asset briefs, approved art formats, SVG/PNG/HTML/Markdown asset production, image-generation prompts, HTML/SVG storyboards, Telegram preview delivery, visual fit review, and eventual asset pipeline judgment. Use him when Axiomancer needs art direction, game-tailored asset generation, or review of generated visuals. See `references/toadvine-art-pipeline.md`.
- Glanton — Nexus State Marshal: reconciles T/Hermes decisions with repo-local Nexus machinery, including `/march`, `/oversight`, phase candidates, critique/audit logs, build plans, repo skills, and central ledger doctrine. Use him when autonomous workers may be acting from stale state or when chat decisions must become durable repo marching law. See `references/glanton-nexus-state-marshal.md`.

- Glanton — Nexus State Marshal: reconciliation of Hermes/T decisions with repo-local Nexus machinery (`/march`, `/oversight`, `plan/steps/01_build_plan.md`, `PHASE_CANDIDATES.md`, `CRITIQUE.md`, `AUDIT.md`), source-of-truth hierarchy, stale command-state drift, and ADR/CDR decision-record discipline.
- The Mennonite — Requirements and Risk Examiner: SomberSoft specialist for premortems, assumption testing, contradiction/undefined-term checks, acceptance and abort criteria, and proportional PRDs/BDDs/TDDs/worker packets. Every HOLD must name the evidence or mitigation that could permit GO. When T asks to add work/TODOs/phases, use the SomberSoft Phase Forge at `~/Workspace/SomberSoft/sombersoft-phases/`: capture raw ideas in `INTAKE.md`, gather relevant specialist opinions, record verdicts when substantial, have the Mennonite test readiness and create only proportional docs under `specs/` and/or worker packets under `packets/`, then promote only Definition-of-Ready work into `PHASES.md` with document references.
- Bathcat — Field Intelligence and Knowledge Scout: SomberSoft specialist for technical/market/mechanics reconnaissance, prior-art discovery, source trails, BGG/forums/rulebooks, retrieval gaps, and knowledge-base provenance. He separates witnessed fact, interpretation, hearsay, and unknown; field instinct creates leads but never substitutes for citation.

Useful future roles:

- Gameplay Systems Engineer — mechanics, combat loops, progression, simulation.
- Mobile UI Engineer — React Native/component architecture, accessibility, polish.
- Narrative/Lore Designer — philosophical themes, quest/event text, world consistency.
- AI Workflow Architect — agent pipelines, skills, automations, evaluation loops.
- QA/Test Specialist — hermetic tests, regression coverage, release checks.

When delegating, pass each specialist the relevant repo path, task goal, constraints, and expected verification artifacts.

When recurring work exceeds persona-level advice, recommend a separate Hermes Agent/profile/worker instead of only proposing another character. Use this for work that needs independent execution, scheduling, tools, memory, or sustained autonomy. Persona is voice and judgment; another Hermes Agent is operational capacity.

When T asks to give Hermes or SomberSoft agents more operational power through services such as Vercel, Expo/EAS, Supabase, Linear, Sentry/PostHog, Figma, MCP servers, or similar integrations, treat it as company infrastructure work: inspect existing tools/MCP/auth first, document the plan in a stable `~/Workspace/` root file, install/verify CLIs where authorized, handle secrets by name only, and separate preview/read-only authority from production/destructive/public authority. See `references/sombersoft-agent-power-integrations.md`. When the approved power is GitHub Actions release automation for Axiomancer mechanics npm publishing or mobile Expo/EAS preview builds, use `references/sombersoft-release-automation-actions.md` for the manual workflow shapes, secret boundaries, and verification pattern.

When T asks to audit or fix scheduled prompts, old repo paths, cron workers, or unattended pull/update behavior, treat it as scheduler state maintenance. Inspect full cron prompt text, not just previews; search for obsolete path classes; patch jobs through `cronjob(action="update", ...)`; verify with a re-scan and `cronjob list`. If T says "don't use AI", create a deterministic script under `~/.hermes/scripts/` and schedule it with `no_agent=True`. For Axiomancer phase additions, pull current monorepo `main` before touching phase files even though the script-only auto-pull runs every 4 hours. See `references/sombersoft-scheduled-repo-hygiene.md`.

When T asks for GitHub Actions that publish `axiomancer-mechanics` or deploy `axiomancer-mobile` preview builds, use manual `workflow_dispatch` release automation first: mechanics gets a dry-run-default npm publish workflow gated by `npm run verify` + `npm run deploy:check` and `NPM_TOKEN` only for live publish; mobile gets an internal Expo/EAS preview workflow gated by `npm run verify`, optional visual smoke, and `EXPO_TOKEN` only for the EAS step. Do not auto-bump versions, push tags, submit to stores, or trigger live publish/cloud builds unless T explicitly asks. See `references/sombersoft-release-automation-github-actions.md`.

When T asks to improve Hermes itself, configure tools/providers, review unused Hermes capabilities, or take Hermes upgrades one at a time, load `hermes-agent`, inspect live Hermes state, maintain `~/Workspace/SomberSoft/HERMES_UPGRADES.md`, execute a single upgrade at a time, verify it, and patch the ledger before moving on. Keep each upgrade concise: what it is, what it is used for, and what is needed. See `references/hermes-upgrades-ledger.md`.

When T asks whether third-party context compression/proxy tooling such as Headroom would benefit SomberSoft/Hermes, treat it as a contained spike before adoption: install in an isolated venv/workspace, test CLI/proxy/MCP smoke paths, run synthetic and actual Axiomancer evidence probes with exact sentinels, record token savings and preservation failures, stop any background proxy, write the spike report, and patch `~/Workspace/SomberSoft/HERMES_UPGRADES.md`. Do not wire compression/proxy tools into the main Judge Telegram profile first; prove them on backend worker lanes where noisy logs and read-heavy audits dominate. See `references/sombersoft-context-compression-spikes.md`.

When T asks to create, tune, refresh, upgrade, or use SomberSoft gang personas as real Hermes workers, treat it as durable profile work. If profiles already exist, do not pretend the work is new creation; perform a profile-builder-grade refresh instead: inspect `hermes profile list/show`, review each profile's `SOUL.md`, description, toolsets, skills, MCPs, model/reasoning budget, `terminal.cwd`, memories, and role-labeled cron jobs. Patch only the role under discussion unless T explicitly orders a full-gang pass. For new profiles, create named profiles with `hermes profile create <name> --clone-all`, write role-specific `SOUL.md` files, tune toolsets by role, set role-owned cwd/reasoning/max-turn/timeout/environment-hint budgets, verify configs plus important MCPs and scheduler state, and record the change in `~/Workspace/SomberSoft/HERMES_UPGRADES.md`. Do not flatten all profiles onto the strongest model: current law is Sol for default Judge, `judge`, and `tobin`; Luna for `kid`, `toadvine`, `glanton`, `webster`, and `governor`. Audit explicit cron model overrides separately because a role-labeled job can otherwise inherit the default Judge model. For project-local MCP requests, do not guess which `.mcp.json` entry T means: locate the exact named server, sync clean `main` if the server has landed remotely, translate its command to absolute paths, configure default Judge and named `judge` separately when applicable, and require `config check` + `mcp test` + `mcp list` for every target profile. Non-interactive `mcp add` must explicitly answer the tool-enable prompt or discovery may succeed while saving nothing. See `references/hermes-profile-project-mcp-wiring.md`. Do not confuse temporary `delegate_task` subagents with durable profiles, and do not create separate Telegram bots unless T asks. See `references/sombersoft-specialist-hermes-profiles.md`.

When T asks whether free global npm packages could improve Hermes, the Judge, or SomberSoft specialist profiles, treat it as Hermes/SomberSoft operational tooling: load `hermes-agent`, inspect live Node/npm/global prefix/PATH state, recommend only free/open-source CLI tools with role-specific leverage, install only authorized sets, fix PATH if global npm bins are hidden, verify `command -v` plus versions, and update `~/Workspace/SomberSoft/HERMES_UPGRADES.md`. For memory-related npm packages, do not blindly install; decide between native Hermes memory providers and an MCP memory server architecture first. If configuring `@modelcontextprotocol/server-memory`, answer the MCP enable prompt non-interactively, set a deliberate `MEMORY_FILE_PATH`, verify with `hermes mcp test memory`, and remember that new MCP tools require a fresh session/gateway restart before use. See `references/sombersoft-hermes-global-npm-tooling.md`.

When T asks to seed, initialize, audit, or "get the ball rolling" on SomberSoft/Hermes memory systems, act rather than asking if the yes is obvious. Load `hermes-agent`, inspect live memory/MCP state, seed Holographic memory from durable doctrine (`VISION.md`, central ledger, CDRs/ADRs) using compact class-level facts, seed the MCP memory graph with entity/relation structure rather than flat trivia, probe/contradict/cleanup duplicates where tools permit, and report verified counts/status. See `references/sombersoft-memory-seeding.md`.

Before hiring a new specialist for a proposed capability, check whether the supporting workflow/infrastructure already exists. If the user wants an agentic role such as automated playtester, build or inspect the harness first, then hire/name the persona to inhabit that system. Do not substitute lore for tooling. If the capability is sustained research across RPG mechanics, AI workflows, papers, competitors, and tooling, recommend a dedicated research specialist whose briefs end in verdicts and next tests rather than link piles.

## Blood Meridian persona and narrative influence

When T asks to make SomberSoft agents more faithful to their *Blood Meridian* characters or make Axiomancer narration strongly influenced by the novel without plagiarism, use a two-layer system: chapter/page-grounded OKF evidence in SomberSoft-Memory, then compact operational persona cards and separate Axiomancer narrative law. Never load chapter summaries wholesale into live prompts. Enforce the anti-fluff ratio of 85–90% task content, 10–15% voice/behavioral pressure, and near-zero ornament; character must change evidence and decisions, not merely diction. Pilot one persona and one scene before broad rollout. See `references/blood-meridian-persona-and-narrative-corpus.md`.

## Memory and skills discipline

- Save durable user/project preferences as memory when they will matter across sessions.
- Do not save transient progress such as commit SHAs, PR numbers, or one-off task status.
- If a reusable SomberSoft/Axiomancer workflow emerges, update this skill or add a concise reference under `references/` instead of creating narrow one-off skills.

## References

- `references/sombersoft-monorepo-and-kb-isolation.md` — active repo layout after Axiomancer monorepo migration, SomberSoft-Memory `.gitignore` hygiene, isolated `game-knowledge-base` repo ownership, Governor cron commit/push doctrine, and `/march` GitHub Action secret note.
- `references/sombersoft-scheduled-repo-hygiene.md` — audit/update live scheduled prompts after repo moves, create script-only `no_agent` pull jobs, verify cron JSON/listing, and enforce Governor/Axiomancer pull-before-write discipline.
- `references/third-party-card-library-discovery.md` — find machine-readable digital card-game libraries for Axiomancer research; includes Dawncaster Blightbane API and Neurrone SQLite DB anchors.
- `references/sombersoft-external-reference-asset-imports.md` — import external reference assets into SomberSoft/Axiomancer planning dirs with source provenance, exclusion-count verification, disposable clone cleanup, and retroactive PR correction when a direct main push must become a review branch.

- `references/sombersoft-cron-prompt-and-toolset-updates.md` — update live cron prompts/toolsets through cron management, verify actual run outputs, and include `delegation` when scheduled workers must spawn subagents.
- `references/kid-encounter-agents/hazard-combat-first-level.md` — Kid route-first combat doctrine: walk real `fishing-village` first-level nodes, trigger authored encounters, choose `FIGHT`, and verify in-place Hazard-style combat; legacy `/combat` is fallback/regression evidence only.
- `references/axiomancer-game-cli-route-hazard-handoff.md` — mechanics `game.cli` route-to-Hazard handoff/export pattern and verification witness (`fv-2,fv-12` → `hazardCombat:*`).
- `references/toadvine-art-pipeline.md` — Toadvine art direction, approved asset formats, Telegram preview bundle, generated-art provenance, and when hosting becomes worthwhile.
- `references/onboarding-context.md` — initial user-provided context about SomberSoft, Axiomancer, and desired assistant/agent relationship.
- `references/axiomancer-playtest-evidence.md` — reusable pattern for engine-level automated playtest evidence, combat/friendship metrics, resolver log pitfalls, and Tobin-readable design conclusions.
- `references/sombersoft-command-ledger-and-rituals.md` — central ledger location, company thesis/lanes, aggressive jurisdiction, recurring ritual schedules, ET/UTC scheduling note, report paths, worker lanes, and specialist-vs-Hermes-agent recruitment doctrine.
- `references/sombersoft-doctrine-sweeper.md` — run the weekly doctrine sweeper: inspect recent roundtable/playthrough/alignment reports, promote only settled repeated doctrine, patch repo-local entrypoints when obvious, write the dated sweeper report, and avoid cron/deploy/public side effects.
- `references/toadvine-art-direction-and-asset-pipeline.md` — Toadvine's approved role as Brutalist Art Director and Asset Marshal, including art-generation capability, approved formats, asset record requirements, aesthetic law, and standalone persona prompt.
- `references/axiomancer-temporary-encounter-shell-phase.md` — how to queue temporary non-combat Event encounter shell phases for `/march`, document scaffolding status, and avoid prematurely designing final minigames.
- `references/axiomancer-agenda-and-evidence-decisions.md` — morning-agenda decisions: reference screenshot storage, event sequencing, combat trigger bug framing, deferred minigame direction, and Expo deployment access guardrails.
- `references/sombersoft-agent-power-integrations.md` — class pattern for expanding Hermes/SomberSoft operational power through CLIs, MCP, service auth, workspace-root roadmaps, secret handling, authority boundaries, and research-specialist signals.
- `references/axiomancer-mobile-combat-ux-tuning-automation.md` — Mobile combat UX tuning pattern: automated input-friction A/B evidence, no-question worker runs, VITAE/STANCE copy guard, branch+PR delivery, and manual GitHub Actions runner shape.
- `references/hermes-upgrades-ledger.md` — maintain `~/Workspace/SomberSoft/HERMES_UPGRADES.md`, inspect live Hermes state, execute one upgrade at a time, verify, and patch the ledger before proceeding.
- `references/sombersoft-context-compression-spikes.md` — evaluate context-compression/proxy tools such as Headroom through isolated spikes, evidence-preservation probes, worker-lane-first adoption, and upgrade-ledger capture.
- `references/axiomancer-cross-repo-combat-audit.md` — audit pattern for stale local branches, advanced `origin/main`, conflicting historical PRs, and salvage-before-reset repo cleanup after autonomous Axiomancer work.
- `references/sombersoft-doctrine-alignment-audit.md` — cross-repo audit pattern for comparing central SomberSoft doctrine, CDRs/ADRs, Nexus docs, mechanics code, and mobile code into a durable alignment verdict.
- `references/axiomancer-march-claude-actions-permissions.md` — verify and repair `/march` GitHub Actions permission bypass for Claude Code base action, including the supported `settings.permissions.defaultMode: "bypassPermissions"` pattern.
- `references/axiomancer-march-manual-phase-focus.md` — add an optional manual `focus_phase` input to March so T can dispatch `/ship-a-phase phase <id>` from the Actions tab while scheduled runs keep normal `/march` behavior.
- `references/axiomancer-mobile-visual-baseline-approval.md` — approve current smoke screenshots as committed visual baselines, verify the visual gate, and push `main`.
- `references/sombersoft-adr-cdr-decision-records.md` — class-level pattern for installing company CDRs and repo ADRs so durable SomberSoft/Axiomancer decisions stop drifting between chat, memory, ledgers, plans, and Nexus state.
- `references/axiomancer-combat-resource-truth.md` — canonical resource-state shape for skill casting, implementation anchors, stale-doc traps, and targeted verification pattern for resource/skill availability audits.
- `references/axiomancer-roster-wide-difficulty-tuning-loop.md` — Phase 107 balance loop: preflight token resources/skills/status effects, parameter tuning first, repeated playtests, ~70% win-rate target, and mechanics-change stop rule.
- `references/glanton-nexus-state-marshal.md` — Glanton's Nexus state reconciliation role, source-of-truth hierarchy, `/march`/`/oversight` drift audit pattern, and proposed guardrail phase shape.
- `references/glanton-overnight-drift-promotion.md` — overnight pattern for resolving safe drift, promoting code-heavy doctrine breaks above the queue, writing morning reports, and scheduling autonomous workers with clear authority boundaries.
- `references/axiomancer-cross-repo-mechanics-release.md`
- `references/axiomancer-mechanics-release-to-mobile-phase.md` — concrete handoff pattern for creating a Mobile catch-up phase and upgrade guide after a mechanics npm release. — publish mechanics releases as cross-repo contract events: verify/publish/confirm registry truth, update release logs, document mobile upgrade obligations, and avoid false "published" claims when npm is still behind.
- `references/sombersoft-specialist-hermes-profiles.md` — create and tune SomberSoft gang Hermes profiles, including profile roster, `SOUL.md` setup, role-specific tool loadouts, verification, and pitfalls around delegate_task vs durable profiles.
- `references/sombersoft-specialist-cron-and-skill-profile-updates.md` — update specialist scheduled rituals and profile skill logic safely: patch active cron/skill truth first, mirror into specialist profiles, validate cron JSON, verify with `cronjob list`, and finish partial Claude handoffs before the next run.
- `references/sombersoft-hermes-global-npm-tooling.md` — free global npm tooling for Hermes/SomberSoft profiles, PATH repair pattern, verification probes, and memory-package/MCP architecture cautions.
- `references/sombersoft-memory-seeding.md` — seed and verify SomberSoft memory across Holographic facts and MCP memory graph from durable doctrine sources without asking when the yes is obvious.
- `references/sombersoft-workspace-consolidation.md` — consolidate loose SomberSoft workspace artifacts into `~/Workspace/SomberSoft`, excluding `axiomancer-both`, then commit/push/verify the central memory repo.
- `references/axiomancer-labyrinth-reference-mining.md` — literal Act II Labyrinth doctrine and source-mining pattern for *Maze*, David Bowie *Labyrinth*, and Mörk Borg: extract symbolic navigation grammar, not copied content.
- `references/sombersoft-github-branch-pr-policy.md` — branch vs main defaults, mandatory push+PR behavior when T asks for a branch, PR description standards, repo-tailored PR template expectations, and the retroactive-PR pitfall when work was already pushed to `main`.
- `references/axiomancer-mobile-dev-menu-populate.md` — evaluate Mobile dev-menu POPULATE and similar controls as evidence fixtures; ensure UI-state work gets deterministic representative data, especially rarity/affix showcases.
- `references/axiomancer-cross-repo-doctrine-to-mobile-design-doc.md` — translate accepted mechanics doctrine into implementation-ready mobile design docs while preserving mechanics as rules authority and mobile as presentation authority.
- `references/axiomancer-cross-repo-hazard-doctrine-dissemination.md` — propagate accepted Hazard minigame doctrine across mechanics docs, mobile design/phase docs, and central SomberSoft CDR/ledger, especially when delegating one Claude Code worker per repo.
- `references/axiomancer-equipment-ownership-and-release-provenance.md` — verify Phase 154-style equipment ownership migrations, mobile package installs, engine-owned loot/equip exports, and npm/git/tag release-provenance drift without inventing a release story.

